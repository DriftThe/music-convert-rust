# Tauri + Vue + TypeScript

A project built with Tauri that can decrypt .ncm files downloaded from NetEase Cloud Music and convert the bit rate of music files.

Just for learning Rust myself.

## Debug method
<code>npm run tauri dev</code>

## Build method
<code>npm run tauri build</code>
